name: Enterprise Egress Gateway HA on AKS (ci-egress-gateway-ha-aks)

on:
  schedule:
    - cron: "0 0 * * 1-5"

  workflow_dispatch:
    inputs:
      PR-number:
        description: "Pull request number."
        required: true
      context-ref:
        description: "Context in which the workflow runs. If PR is from a fork, will be the PR target branch (general case). If PR is NOT from a fork, will be the PR branch itself (this allows committers to test changes to workflows directly from PRs)."
        required: true
      SHA:
        description: "SHA under test (head of the PR branch)."
        required: true
      base-SHA:
        description: "SHA of the base branch (target branch of the PR)."
        required: false
      extra-args:
        description: "[JSON object] Arbitrary arguments passed from the trigger comment via regex capture group. Parse with 'fromJson(inputs.extra-args).argName' in workflow."
        required: false
        default: "{}"

# For testing uncomment following lines:
#  push:
#    branches:
#      - your_branch_name

permissions:
  # To be able to access the repository with actions/checkout
  contents: read
  # Required to generate OIDC tokens for `az` authentication
  id-token: write
  # To allow retrieving information from the PR API
  pull-requests: read
  # To be able to set commit status
  statuses: write

concurrency:
  # Structure:
  # - Workflow name
  # - Event type
  # - A unique identifier depending on event type:
  #   - schedule: SHA
  #   - workflow_dispatch: PR number
  #
  # This structure ensures a unique concurrency group name is generated for each
  # type of testing, such that re-runs will cancel the previous run.
  group: |
    ${{ github.workflow }}
    ${{ github.event_name }}
    ${{
      (github.event_name == 'push' && github.sha) ||
      (github.event_name == 'schedule' && github.sha) ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.PR-number)
    }}
  cancel-in-progress: true

env:
  # renovate: datasource=github-releases depName=kubernetes/kubernetes
  kubectl_version: v1.36.2

  test_name: egwha-aks
  cost_reduction: --node-vm-size Standard_B2s --node-osdisk-size 30

  peer_asn: 65001
  ipam_cidr: 10.0.100.1/30
  conn_disrupt_egw_ha_egress_cidrs: 10.0.101.1/30,10.0.102.1/30
jobs:
  echo-inputs:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    name: Echo Workflow Dispatch Inputs
    runs-on: ubuntu-24.04
    steps:
      - name: Echo Workflow Dispatch Inputs
        run: |
          echo '${{ tojson(inputs) }}'

  commit-status-start:
    name: Commit Status Start
    runs-on: ubuntu-24.04
    steps:
      - name: Set initial commit status
        uses: myrotvorets/set-commit-status-action@3730c0a348a2ace3c110851bed53331bc6406e9f # v2.0.1
        with:
          sha: ${{ inputs.SHA || github.sha }}

  wait-for-images:
    name: Wait for images
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout context ref (trusted)
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ inputs.context-ref || github.sha }}
          persist-credentials: false
      - name: Wait for images
        uses: ./.github/actions/wait-for-images
        with:
          SHA: ${{ inputs.SHA || github.sha }}
          images: cilium-ci operator-generic-ci cilium-cli-ci

  install-and-connectivity:
    runs-on: ubuntu-24.04
    name: Setup and Test
    needs: wait-for-images
    timeout-minutes: 120

    strategy:
      fail-fast: false
      matrix:
        include:
          - id: 0
            name: 'multi pool IPAM'
            ipam: 'multi-pool'

          - id: 1
            name: 'cluster pool IPAM'
            ipam: 'cluster-pool'

    steps:
      - name: Checkout context ref (trusted)
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ inputs.context-ref || github.sha }}
          persist-credentials: false

      - name: Set Environment Variables
        uses: ./.github/actions/set-env-variables

      - name: Set up job variables
        id: vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] ; then
            SHA="${{ inputs.SHA }}"
          else
            SHA="${{ github.sha }}"
          fi

          CILIUM_DOWNGRADE_VERSION=$(contrib/scripts/print-downgrade-version.sh stable)
          echo "CILIUM_DOWNGRADE_VERSION: ${CILIUM_DOWNGRADE_VERSION}"
          if [ -z "${CILIUM_DOWNGRADE_VERSION}" ]; then
            echo "::notice::No CILIUM_DOWNGRADE_VERSION returned; skipping upgrade/downgrade steps"
          fi

          MATRIX_ID=${{ matrix.id }}
          NAME="${{ env.test_name }}-${{ github.run_id }}-${{ github.run_attempt }}-${MATRIX_ID}"

          CILIUM_INSTALL_DEFAULTS=" \
            --wait=false \
            --set=debug.enabled=true \
            --set=cluster.name=${NAME} \
            --set=azure.resourceGroup=${NAME} \
            --datapath-mode=aks-byocni \
            --set=enterprise.egressGatewayHA.enabled=true \
            --set=enterprise.egressGatewayHA.socketTermination.enabled=true \
            --set=enterprise.bgpControlPlane.enabled=true \
            --set=enterprise.dnsProxyHA.enabled=true \
            --set=enableIPv4Masquerade=true \
            --set=bpf.masquerade=true \
            --set=kubeProxyReplacement=true \
            --set=ipv4.enabled=true \
            --set=ipv6.enabled=true \
            --set=updateStrategy.rollingUpdate.maxUnavailable=1 \
            --set=minReadySeconds=15 \
            --nodes-without-cilium
          "

          BASE_FEATURE_GATES="EgressGatewayHA,EgressGatewayHASocketTermination,DNSProxyHA,DatapathIPModeDualStack"

          if [ "${{ matrix.ipam }}" == "cluster-pool" ]; then
            FEATURE_GATES="${BASE_FEATURE_GATES}"

            CILIUM_INSTALL_DEFAULTS+=" --set=ipam.mode=cluster-pool \
                                      --set=ipam.operator.clusterPoolIPv4PodCIDRList=192.168.48.0/20 \
                                      --set=ipam.operator.clusterPoolIPv6PodCIDRList=fd00::/104 "
          fi

          if [ "${{ matrix.ipam }}" == "multi-pool" ]; then
            FEATURE_GATES="${BASE_FEATURE_GATES},IPAMMultiPool"

            CILIUM_INSTALL_DEFAULTS+=" --set=ipam.mode=multi-pool \
                                      --set=ipam.operator.autoCreateCiliumPodIPPools.default.ipv4.cidrs='{192.168.32.0/20}' \
                                      --set=ipam.operator.autoCreateCiliumPodIPPools.default.ipv4.maskSize=24 \
                                      --set=ipam.operator.autoCreateCiliumPodIPPools.default.ipv6.cidrs='{fd00::/120}' \
                                      --set=ipam.operator.autoCreateCiliumPodIPPools.default.ipv6.maskSize=128 \
                                      --set=ipam.operator.autoCreateCiliumPodIPPools.client2-pool.ipv4.cidrs='{192.168.16.0/20}' \
                                      --set=ipam.operator.autoCreateCiliumPodIPPools.client2-pool.ipv4.maskSize=27 \
                                      --set=ipam.operator.autoCreateCiliumPodIPPools.client2-pool.ipv6.cidrs='{fd00::300/120}' \
                                      --set=ipam.operator.autoCreateCiliumPodIPPools.client2-pool.ipv6.maskSize=122 "
          fi

          CILIUM_INSTALL_DEFAULTS+=" --set=enterprise.featureGate.approved='{${FEATURE_GATES}}' "

          CILIUM_INSTALL_NEWEST=" \
            --chart-directory=untrusted/cilium-newest/install/kubernetes/cilium \
            ${CILIUM_INSTALL_DEFAULTS} \
            --set=image.override=quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/cilium-ci:${SHA} \
            --set=operator.image.override=quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/operator-generic-ci:${SHA}
          "

          CONNECTIVITY_TEST_DEFAULTS=" \
            --hubble=false \
            --log-code-owners --code-owners=${CILIUM_CLI_CODE_OWNERS_PATHS} \
            --exclude-code-owners=${CILIUM_CLI_EXCLUDE_OWNERS} \
            --flow-validation=disabled \
            --include-unsafe-tests \
            --collect-sysdump-on-failure \
            --log-check-extra-exceptions=cleanup-peer-config-status \
            --log-check-extra-exceptions='received notification' \
            --log-check-extra-exceptions='failed to send'
          "

          if [ "${{ matrix.ipam }}" == "multi-pool" ]; then
            CONNECTIVITY_TEST_DEFAULTS+=" --deployment-pod-annotations='{ \
                \"client2\":{\"ipam.cilium.io/ip-pool\":\"client2-pool\"} \
            }' "
          fi

          OWNER="${{ github.ref_name }}"
          OWNER="${OWNER//[.\/]/-}"

          aks_version_and_location=$(yq '.include | sort_by(.version) | reverse | .[0] | "\(.version),\(.location)"' .github/actions/azure/k8s-versions.yaml)
          AKS_VERSION=$(echo $aks_version_and_location | cut -d',' -f1)
          AKS_LOCATION=$(echo $aks_version_and_location | cut -d',' -f2)

          echo sha=${SHA} >> $GITHUB_OUTPUT
          echo downgrade_version=${CILIUM_DOWNGRADE_VERSION} >> $GITHUB_OUTPUT
          echo cilium_install_defaults=${CILIUM_INSTALL_DEFAULTS} >> $GITHUB_OUTPUT
          echo cilium_install_newest=${CILIUM_INSTALL_NEWEST} >> $GITHUB_OUTPUT
          echo connectivity_test_defaults=${CONNECTIVITY_TEST_DEFAULTS} >> $GITHUB_OUTPUT
          echo name=${NAME} >> $GITHUB_OUTPUT
          echo owner=${OWNER} >> $GITHUB_OUTPUT
          echo aks_version=${AKS_VERSION} >> $GITHUB_OUTPUT
          echo aks_location=${AKS_LOCATION} >> $GITHUB_OUTPUT

      - name: Install kubectl
        run: |
          curl -sLO "https://dl.k8s.io/release/${{ env.kubectl_version }}/bin/linux/amd64/kubectl"
          curl -sLO "https://dl.k8s.io/${{ env.kubectl_version }}/bin/linux/amd64/kubectl.sha256"
          echo "$(cat kubectl.sha256)  kubectl" | sha256sum --check
          sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
          kubectl version --client

      - name: Login to Azure
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          creds: ${{ secrets.AZURE_PR_SP_CREDS }}

      - name: Display version info of installed tools
        run: |
          echo "--- kubectl ---"
          kubectl version --client

      - name: Configure az command
        run: |
          az extension add --name aks-preview
          az extension update --name aks-preview
          az feature register --namespace "Microsoft.ContainerService" --name "KubeProxyConfigurationPreview"
          az feature show --namespace "Microsoft.ContainerService" --name "KubeProxyConfigurationPreview"
          az provider register --namespace Microsoft.ContainerService

      - name: Get Runner IP
        id: get-runner-ip
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const net = require('net');
            const url = 'https://whatismyip.azurewebsites.net/json/';
            const res = await fetch(
              url, {
                method: 'GET',
                headers: {
                  'Content-Type': 'application/json'
                }
              });
            if (!res.ok) {
              throw new Error(`Failed to fetch IP address: ${res.status} ${res.statusText}`);
            }
            const data = await res.json();
            const ip = data.ip;
            if (!net.isIP(ip)) {
              throw new Error(`Invalid IP address: ${ip}`);
            }
            return `${ip}/32`;

      - name: Checkout ${{ steps.vars.outputs.downgrade_version }} branch to get the Helm chart
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ steps.vars.outputs.downgrade_version }}
          persist-credentials: false
          path: untrusted/cilium-downgrade
          sparse-checkout: |
            install/kubernetes/cilium

      - name: Retrieve downgrade target SHA
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        id: downgrade-branch
        run: |
          echo "sha=$(cd untrusted/cilium-downgrade && git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: Wait for images to be available (downgrade)
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        timeout-minutes: 10
        shell: bash
        run: |
          for image in cilium-ci operator-generic-ci ; do
            until docker manifest inspect quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/$image:${{ steps.downgrade-branch.outputs.sha }} &> /dev/null; do sleep 45s; done
          done

      - name: Create AKS cluster
        run: |
          # Create group
          az group create \
            --name ${{ steps.vars.outputs.name }} \
            --location ${{ steps.vars.outputs.aks_location }} \
            --tags usage=${{ github.repository_owner }}-${{ github.event.repository.name }} owner=${{ steps.vars.outputs.owner }}

          az network vnet create \
            --resource-group ${{ steps.vars.outputs.name }} \
            --location ${{ steps.vars.outputs.aks_location }} \
            --name ${{ env.test_name }}-vnet \
            --address-prefixes 192.168.8.0/22 fd00::/48

          EGW_SUBNET_ID=$(az network vnet subnet create \
            --resource-group ${{ steps.vars.outputs.name }} \
            --vnet-name ${{ env.test_name }}-vnet \
            --name egw_subnet \
            --address-prefixes 192.168.10.0/24 fd00:0:0:10::/64 \
            --query "id" \
            -o tsv)

          EXTERNAL_SUBNET_ID=$(az network vnet subnet create \
            --resource-group ${{ steps.vars.outputs.name }} \
            --vnet-name ${{ env.test_name }}-vnet \
            --name external-subnet \
            --address-prefixes 192.168.11.0/24 fd00:0:0:11::/64 \
            --query "id" -o tsv)

          # Create AKS cluster
          az aks create \
            --resource-group ${{ steps.vars.outputs.name }} \
            --name ${{ steps.vars.outputs.name }} \
            --location ${{ steps.vars.outputs.aks_location }} \
            --kubernetes-version ${{ steps.vars.outputs.aks_version }} \
            --network-plugin none \
            --nodepool-name egwnodepool \
            --nodepool-labels egress-group=test rr-role=client \
            --kube-proxy-config ./.github/enterprise-egw-ha/kube-proxy.json \
            --node-count 3 \
            --ip-families ipv4,ipv6 \
            --tags svic_falco_supported=no \
            --vnet-subnet-id $EGW_SUBNET_ID \
            ${{ env.cost_reduction }} \
            --generate-ssh-keys \
            --api-server-authorized-ip-ranges ${{ steps.get-runner-ip.outputs.result }}

          az aks nodepool add \
            --resource-group ${{ steps.vars.outputs.name }} \
            --cluster-name ${{ steps.vars.outputs.name }} \
            --name rrnodepool \
            --node-count 2 \
            --labels rr-role=route-reflector \
            --tags svic_osquery_supported=no \
            --node-vm-size Standard_D4ds_v4 \
            --vnet-subnet-id $EGW_SUBNET_ID

          az aks nodepool add \
            --resource-group ${{ steps.vars.outputs.name }} \
            --cluster-name ${{ steps.vars.outputs.name }} \
            --name nociliumpool \
            --node-count 1 \
            --labels cilium.io/no-schedule=true \
            --node-taints "cilium.io/no-schedule=true:NoSchedule" \
            --tags svic_osquery_supported=no \
            --node-vm-size Standard_D4ds_v4 \
            --vnet-subnet-id $EXTERNAL_SUBNET_ID

      - name: Get cluster credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ steps.vars.outputs.name }} \
            --name ${{ steps.vars.outputs.name }}

      - name: Generate cilium-cli kubeconfig
        id: gen-kubeconfig
        uses: ./.github/actions/get-cloud-kubeconfig
        with:
          kubeconfig: "~/.kube/config"

      - name: Install Cilium CLI
        uses: cilium/cilium-cli@e391e739db83a8b888ecf18786af6853c51a0c58 # v0.19.5
        with:
          skip-build: ${{ env.CILIUM_CLI_SKIP_BUILD }}
          image-repo: ${{ env.CILIUM_CLI_IMAGE_REPO }}
          image-tag: ${{ steps.vars.outputs.sha }}
          repository: ${{ env.CILIUM_CLI_RELEASE_REPO }}
          release-version: ${{ env.CILIUM_CLI_VERSION }}
          kubeconfig: ${{ steps.gen-kubeconfig.outputs.kubeconfig_path }}

      # Warning: since this is a privileged workflow, subsequent workflow job
      # steps must take care not to execute untrusted code.
      - name: Checkout pull request branch (NOT TRUSTED)
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ steps.vars.outputs.sha }}
          persist-credentials: false
          path: untrusted/cilium-newest
          sparse-checkout: |
            install/kubernetes/cilium

      - name: Set up downgrade settings
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        id: downgrade-vars
        run: |
          CILIUM_INSTALL_STABLE=" \
            --chart-directory=untrusted/cilium-downgrade/install/kubernetes/cilium \
            ${{ steps.vars.outputs.cilium_install_defaults }} \
            --set=image.override=quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/cilium-ci:${{ steps.downgrade-branch.outputs.sha }} \
            --set=operator.image.override=quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/operator-generic-ci:${{ steps.downgrade-branch.outputs.sha }}
          "
          echo cilium_install_stable=${CILIUM_INSTALL_STABLE} >> $GITHUB_OUTPUT

      - name: Install Cilium ${{ steps.vars.outputs.downgrade_version }}
        id: install-cilium
        run: |
          K8S_API_SRV_ADDR=$(kubectl config view -o jsonpath='{.clusters[0].cluster.server}' | sed 's|https://||')
          echo "Retrieved Kubernetes API Server address: '${K8S_API_SRV_ADDR}'"

          CLUSTER_FLAGS="--set=k8sServiceHost=${K8S_API_SRV_ADDR} --set=k8sServicePort=443"

          if [ -n "${{ steps.vars.outputs.downgrade_version }}" ]; then
            cilium install --dry-run-helm-values ${{ steps.downgrade-vars.outputs.cilium_install_stable }} ${CLUSTER_FLAGS}
            cilium install ${{ steps.downgrade-vars.outputs.cilium_install_stable }} ${CLUSTER_FLAGS}
          else
            cilium install --dry-run-helm-values ${{ steps.vars.outputs.cilium_install_newest }} ${CLUSTER_FLAGS}
            cilium install ${{ steps.vars.outputs.cilium_install_newest }} ${CLUSTER_FLAGS}
          fi

          echo k8s_api_srv_addr=${K8S_API_SRV_ADDR} >> $GITHUB_OUTPUT

      # This installs a dummy CNI on nodes labeled cilium.io/no-schedule=true
      # to make kubelet report them as Ready.
      # Without a CNI, such nodes stay NotReady and AKS auto-repair restarts them.
      - name: Install dummy CNI on node without Cilium
        run: |
          JOB_NAME="inject-dummy-cni"

          cat <<YAML | kubectl apply -f -
          apiVersion: batch/v1
          kind: Job
          metadata:
            name: ${JOB_NAME}
            namespace: kube-system
          spec:
            backoffLimit: 0
            template:
              metadata:
                labels:
                  app: inject-dummy-cni
              spec:
                hostNetwork: true
                nodeSelector:
                  cilium.io/no-schedule: "true"
                tolerations:
                - operator: Exists
                restartPolicy: Never
                containers:
                - name: writer
                  image: quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/cilium-ci:${{ steps.vars.outputs.sha }}
                  securityContext:
                    privileged: true
                    runAsUser: 0
                  command: ["/bin/sh","-c"]
                  args:
                  - |
                    set -eu
                    mkdir -p /host/etc/cni/net.d
                    cat > /host/etc/cni/net.d/05-dummy.conf <<'EOF'
                    {
                      "cniVersion": "0.3.1",
                      "name": "dummy",
                      "type": "dummy-cni",
                      "log-file": "/var/run/dummy.log"
                    }
                    EOF
                    exit 0
                  volumeMounts:
                  - name: etc-cni
                    mountPath: /host/etc/cni/net.d
                volumes:
                - name: etc-cni
                  hostPath:
                    path: /etc/cni/net.d
                    type: DirectoryOrCreate
          YAML

          kubectl -n kube-system wait --for=condition=complete --timeout=180s job/${JOB_NAME}
          kubectl wait --for=condition=Ready --timeout=180s node -l cilium.io/no-schedule=true
          kubectl get nodes -l cilium.io/no-schedule=true -o wide

      - name: Wait for Cilium status to be ready
        run: |
          cilium status --wait --interactive=false

      - name: Install Cilium DNS proxy
        run: |
          helm install -n kube-system cilium-dnsproxy ./enterprise/fqdn-proxy/installation \
            --set image.repository=quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/cilium-dnsproxy-ci \
            --set image.tag=${{ steps.vars.outputs.sha }} \
            --set metrics.enabled=true \
            --set debug=true
          kubectl rollout status -n kube-system ds/cilium-dnsproxy --watch

      - name: Setup Azure Route Server
        id: setup-azure-route-server
        run: |
          az network vnet subnet create \
            --resource-group ${{ steps.vars.outputs.name }} \
            --vnet-name ${{ env.test_name }}-vnet \
            --name RouteServerSubnet \
            --address-prefixes 192.168.9.0/24

          ARS_SUBNET_ID=$(az network vnet subnet show \
            --resource-group ${{ steps.vars.outputs.name }} \
            --vnet-name ${{ env.test_name }}-vnet \
            --name RouteServerSubnet \
            --query id \
            -o tsv)

          az network public-ip create \
            --resource-group ${{ steps.vars.outputs.name }} \
            --name ars-pip \
            --sku Standard \
            --version IPv4

          az network routeserver create \
            --resource-group ${{ steps.vars.outputs.name }} \
            --name ${{ env.test_name }}-ars \
            --hosted-subnet $ARS_SUBNET_ID \
            --public-ip-address ars-pip

          RR_NODE_IPS=$(kubectl get nodes -l rr-role=route-reflector -o json \
              | jq -r '.items[].status.addresses[]
              | select(.type=="InternalIP").address
              | select(test("^(?:[0-9]{1,3}\\.){3}[0-9]{1,3}$"))')

          i=0
          for ip in $RR_NODE_IPS; do
            az network routeserver peering create \
              --resource-group ${{ steps.vars.outputs.name }} \
              --routeserver ${{ env.test_name }}-ars \
              --name peer-aks-rrnode-$i \
              --peer-ip "$ip" \
              --peer-asn ${{ env.peer_asn }}
            i=$((i+1))
          done

          json=$(az network routeserver show \
            --resource-group ${{ steps.vars.outputs.name }} \
            --name ${{ env.test_name }}-ars \
            -o json)

          read -r ARS_ASN ARS_VRIP1 ARS_VRIP2 < <(
            echo "$json" | jq -r '[.virtualRouterAsn, .virtualRouterIps[0], .virtualRouterIps[1]] | @tsv'
          )

          echo ars_asn=${ARS_ASN} >> $GITHUB_OUTPUT
          echo ars_vrip1=${ARS_VRIP1} >> $GITHUB_OUTPUT
          echo ars_vrip2=${ARS_VRIP2} >> $GITHUB_OUTPUT

      - name: Build conn-disrupt test args
        id: conn-disrupt
        run: |
          CONN_DISRUPT_SETUP_ARGS=" \
            --include-conn-disrupt-test-egw-ha \
            --include-unsafe-tests \
            --conn-disrupt-test-setup \
            --conn-disrupt-dispatch-interval 0ms \
            --conn-disrupt-test-restarts-path ./cilium-conn-disrupt-restarts \
            --conn-disrupt-egw-ha-egress-cidrs ${{ env.conn_disrupt_egw_ha_egress_cidrs }} \
            --egw-bgp-peer-addresses ${{ steps.setup-azure-route-server.outputs.ars_vrip1 }} \
            --egw-bgp-peer-addresses ${{ steps.setup-azure-route-server.outputs.ars_vrip2 }} \
            --egw-bgp-asn ${{ steps.setup-azure-route-server.outputs.ars_asn }}
          "
          CONN_DISRUPT_CHECK_ARGS=" \
            --include-unsafe-tests \
            --include-conn-disrupt-test-egw-ha \
            --collect-sysdump-on-failure \
            --sysdump-hubble-flows-count=1000000 \
            --sysdump-hubble-flows-timeout=5m \
            --sysdump-output-filename 'cilium-sysdump-conn-disrupt-<ts>' \
            --conn-disrupt-test-restarts-path ./cilium-conn-disrupt-restarts \
            --flush-ct \
            --test no-interrupted-connections-for-enterprise
          "
          echo conn_disrupt_setup_args=${CONN_DISRUPT_SETUP_ARGS} >> $GITHUB_OUTPUT
          echo conn_disrupt_check_args=${CONN_DISRUPT_CHECK_ARGS} >> $GITHUB_OUTPUT

      - name: Setup conn-disrupt before upgrade
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        run: |
          cilium connectivity test ${{ steps.conn-disrupt.outputs.conn_disrupt_setup_args }}

      - name: Upgrade Cilium
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        shell: bash
        run: |
          cilium upgrade --reset-values \
            --helm-set=disableEnvoyVersionCheck=true \
            ${{ steps.vars.outputs.cilium_install_newest }} \
            --set=k8sServiceHost="${{ steps.install-cilium.outputs.k8s_api_srv_addr }}" \
            --set=k8sServicePort=443

          cilium status --wait --interactive=false --wait-duration=10m
          kubectl get pods --all-namespaces -o wide
          kubectl -n kube-system exec daemonset/cilium -c cilium-agent -- cilium-dbg status

      - name: Check conn-disrupt after upgrade
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        run: |
          cilium connectivity test ${{ steps.conn-disrupt.outputs.conn_disrupt_check_args }}

      - name: Run connectivity egress gateway ha test
        run: |
          cilium connectivity test \
            --namespace kube-system \
            ${{ steps.vars.outputs.connectivity_test_defaults }} \
            --test='seq-egress-gateway-ha-ipam-bgp-advertisement' \
            --test='seq-egress-gateway-ha-ipam-bgp-advertisement-with-l7-policy' \
            --test='seq-egress-gateway-ha-ipam-bgp-advertisement-virtual-ip' \
            --test='check-log-errors' \
            --egw-bgp-peer-addresses ${{ steps.setup-azure-route-server.outputs.ars_vrip1 }} \
            --egw-bgp-peer-addresses ${{ steps.setup-azure-route-server.outputs.ars_vrip2 }} \
            --egw-bgp-asn ${{ steps.setup-azure-route-server.outputs.ars_asn }} \
            --egw-ipam-cidrs ${{ env.ipam_cidr }}

      - name: Setup conn-disrupt before maintenance test
        run: |
          cilium connectivity test ${{ steps.conn-disrupt.outputs.conn_disrupt_setup_args }}

      - name: Cordon GW nodes and restart Cilium
        run: |
          kubectl cordon -l egress-group=test,rr-role=client

          # Wait for the operator to observe the cordon and remove the activeGatewayIPs
          for _ in $(seq 30); do
            active=$(kubectl get iegp \
              -o jsonpath='{.items[*].status.groupStatuses[*].activeGatewayIPs[*]}')
            if [ -z "$active" ]; then break; fi
            echo "Waiting for active gateways to be removed (current: $active)"
            sleep 2
          done
          if [ -n "$active" ]; then
            echo "ERROR: IEGPs still have active gateways after 60s: $active"
            kubectl get iegp -o yaml
            exit 1
          fi
          echo "All IEGPs have no active gateways, operator observed cordon"

          kubectl -n kube-system rollout restart daemonset/cilium
          kubectl -n kube-system rollout restart deployment/cilium-operator
          kubectl -n kube-system rollout status  daemonset/cilium --timeout=10m
          kubectl -n kube-system rollout status  deployment/cilium-operator --timeout=5m

          cilium status --wait --interactive=false --wait-duration=10m
          kubectl get pods --all-namespaces -o wide
          kubectl -n kube-system exec daemonset/cilium -c cilium-agent -- cilium-dbg status

          kubectl uncordon -l egress-group=test,rr-role=client

      - name: Check conn-disrupt after maintenance test
        run: |
          cilium connectivity test ${{ steps.conn-disrupt.outputs.conn_disrupt_check_args }}

      - name: Setup conn-disrupt before downgrade
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        run: |
          cilium connectivity test ${{ steps.conn-disrupt.outputs.conn_disrupt_setup_args }}

      - name: Downgrade to Cilium ${{ steps.vars.outputs.downgrade_version }}
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        shell: bash
        run: |
          cilium upgrade --reset-values \
            --helm-set=disableEnvoyVersionCheck=true \
            ${{ steps.downgrade-vars.outputs.cilium_install_stable }} \
            --set=k8sServiceHost="${{ steps.install-cilium.outputs.k8s_api_srv_addr }}" \
            --set=k8sServicePort=443

          cilium status --wait --interactive=false --wait-duration=10m
          kubectl get pods --all-namespaces -o wide
          kubectl -n kube-system exec daemonset/cilium -c cilium-agent -- cilium-dbg status

      - name: Check conn-disrupt after downgrade
        if: ${{ steps.vars.outputs.downgrade_version != '' }}
        run: |
          cilium connectivity test ${{ steps.conn-disrupt.outputs.conn_disrupt_check_args }}

      - name: Post-test information gathering
        if: ${{ !success() && steps.install-cilium.outcome != 'skipped' && steps.install-cilium.outcome != 'cancelled' }}
        run: |
          kubectl get pods --all-namespaces -o wide
          cilium status
          cilium sysdump \
            --output-filename cilium-sysdump-final
          sudo chmod +r cilium-sysdump-final.zip

      - name: Upload artifacts
        if: ${{ !success() && steps.install-cilium.outcome != 'skipped' && steps.install-cilium.outcome != 'cancelled' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cilium-sysdumps
          path: cilium-sysdump-*.zip
          retention-days: 5

      - name: Delete context refs
        if: ${{ always() }}
        run: |
          rm -rf untrusted/

      - name: Clean up AKS
        if: ${{ always() }}
        run: |
          az group delete --name ${{ steps.vars.outputs.name }} --yes --no-wait
        shell: bash {0} # Disable default fail-fast behaviour so that all commands run independently

  commit-status-final:
    if: ${{ always() }}
    name: Commit Status Final
    needs: install-and-connectivity
    runs-on: ubuntu-24.04
    steps:
      - name: Set final commit status
        uses: myrotvorets/set-commit-status-action@3730c0a348a2ace3c110851bed53331bc6406e9f # v2.0.1
        with:
          sha: ${{ inputs.SHA || github.sha }}
          status: ${{ needs.install-and-connectivity.result }}
