apiVersion: isovalent.com/v1
kind: IsovalentBGPAdvertisement
metadata:
  name: web-degraded
  labels:
    advertise: web-degraded
spec:
  advertisements:
    - advertisementType: Service
      service:
        addresses:
          - LoadBalancerIP
      selector:
        matchLabels:
          app: web-degraded
